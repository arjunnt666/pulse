# Getting Started

```bash
cargo build -p pulse-cli
./target/debug/pulse version
./target/debug/pulse demo --ticks 30
```

That runs a tiny in-process server for a few ticks and prints the final tick number.
